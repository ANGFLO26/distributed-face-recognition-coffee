# ============================================
# Coffeehouse Face Recognition System
# Environment Variables Configuration
# ============================================
#
# Copy this file to .env and update the values
# cp .env.example .env
#

# ============================================
# MongoDB Configuration
# ============================================
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DATABASE=coffeehouse_db
MONGODB_USERNAME=
MONGODB_PASSWORD=

# Alternative: Use full MongoDB connection string
# MONGODB_URL=mongodb://localhost:27017/coffeehouse_db
# MONGODB_URL=mongodb://username:password@localhost:27017/coffeehouse_db

# ============================================
# Server Configuration
# ============================================
# TCP Socket Server (for Python client)
SERVER_HOST=0.0.0.0
SERVER_PORT=8888

# HTTP API Server (for Mobile App)
HTTP_HOST=0.0.0.0
HTTP_PORT=8889

# ============================================
# Face Recognition Configuration
# ============================================
# Tolerance: 0.0-1.0 (lower = more strict, higher = more lenient)
# Recommended: 0.6 (default)
FACE_RECOGNITION_TOLERANCE=0.6

# Model: 'hog' (faster) or 'cnn' (more accurate but slower)
# Recommended: 'hog' (default)
FACE_RECOGNITION_MODEL=hog
